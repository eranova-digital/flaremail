# Base URL for the Worker API (no trailing slash).
# Hostname only is fine — https:// is added automatically.
#
# Examples:
#   API_URL=http://localhost:8787
#   API_URL=https://test-worker.example.workers.dev
#   API_URL=test-worker.example.workers.dev
API_URL=http://localhost:8787
