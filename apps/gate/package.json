{
	"name": "@flaremail/gate",
	"version": "0.9.8",
	"private": true,
	"license": "SEE LICENSE IN LICENSE",
	"scripts": {
		"deploy": "flaremail deploy --gate --yes",
		"predev": "node --import tsx ../../apps/cli/src/index.ts sync --if-missing",
		"dev": "wrangler dev",
		"typegen": "wrangler types",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"typescript": "^5.5.2",
		"wrangler": "^4.105.0"
	}
}
