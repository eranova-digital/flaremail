BETTER_AUTH_SECRET=replace-with-openssl-rand-base64-32
BETTER_AUTH_URL=http://localhost:3000
DATABASE_URL=./data/3p-demo.sqlite

# Browser-facing Flaremail (Vite) — authorize uses this so the session cookie applies.
FLAREMAIL_WEB_URL=http://localhost:5173
# Worker origin — token + userinfo (server-to-server).
FLAREMAIL_API_URL=http://localhost:8787

# From Flaremail Management → OIDC clients (shown once on create / regenerate).
FLAREMAIL_CLIENT_ID=
FLAREMAIL_CLIENT_SECRET=
